export type ProductType = {
    id: string
    title: string
    description: string
    price: number
    discount: number
    rating: string
    category: string
    images: string
}

export type updated = {
    id: number;
    title: string;
    image: string;
    date: string;
  }